> ⚠️ Historical implementation plan (2026-05) — retained for reference; current behavior may differ. See `docs/ARCHITECTURE.md` and the source code.

# Sun Depth Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the sun in the analytic sky shader look naturally volumetric rather than like a flat 2D disk.

**Architecture:** Keep the existing full-screen sky pass. Add source-level shader tests, then replace `get_sun()` in `shaders/sky.wgsl` with layered analytic shading: limb falloff, warm core, procedural surface variation, and corona glow.

**Tech Stack:** Rust 2024, cargo tests, WGSL, wgpu/naga for shader parsing.

---

### Task 1: Shader regression test

**Files:**
- Modify: `Cargo.toml`
- Modify: `tests/shader_source_test.rs`

- [ ] **Step 1: Write the failing test**

Add a dev-dependency on `naga` with `wgsl-in`, then add a test that parses `shaders/sky.wgsl` and checks for `sun_limb`, `sun_surface`, and `sun_corona` terms.

- [ ] **Step 2: Run test to verify it fails**

Run: `cargo test --test shader_source_test sky_shader_sun_uses_layered_depth_terms -- --exact`
Expected: FAIL because the current shader does not contain the layered sun terms.

- [ ] **Step 3: Implement the shader change**

Replace `get_sun()` with a shader-only layered sun implementation.

- [ ] **Step 4: Verify targeted tests pass**

Run: `cargo test --test shader_source_test`
Expected: PASS.

- [ ] **Step 5: Verify repo checks**

Run relevant cargo verification (`cargo fmt -- --check`, `cargo check --all-targets`, and targeted tests; full `make checkall` if time allows).
